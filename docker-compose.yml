services:
  newsbot:
    build: .
    container_name: newsbot
    restart: unless-stopped
    env_file: .env
    volumes:
      - newsbot-data:/app/data
    environment:
      - NEWSBOT_DB=/app/data/news.db

volumes:
  newsbot-data:
